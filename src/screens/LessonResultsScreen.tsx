import { ScreenStub } from "../components/ScreenStub";

/**
 * Stub — Lesson Results (pass/fail verdict, unlock outcome, full metrics and
 * comparison with previous attempts).
 * Design: `screens/lesson_results_typekernel/` — implemented in Phase 4.
 */
export default function LessonResultsScreen() {
  return (
    <ScreenStub
      screenId="lesson-results"
      phase={4}
      description="Attempt report: pass/fail verdict, unlock outcome, full metrics and comparison with previous attempts."
    />
  );
}
