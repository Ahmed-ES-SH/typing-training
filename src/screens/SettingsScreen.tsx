import { ScreenStub } from "../components/ScreenStub";

/**
 * Stub — Settings (keyboard layout, appearance, training preferences, data
 * management, application behavior).
 * Design: `screens/settings_typekernel/` — implemented in Phase 7.
 */
export default function SettingsScreen() {
  return (
    <ScreenStub
      screenId="settings"
      phase={7}
      description="System configuration: keyboard layout, appearance, training preferences and data management."
    />
  );
}
