import { ScreenStub } from "../components/ScreenStub";

/**
 * Stub — Custom Lessons (create/edit/delete/practice user-defined lessons,
 * JSON import/export).
 * Design: `screens/custom_lessons_typekernel/` — implemented in Phase 7.
 */
export default function CustomLessonsScreen() {
  return (
    <ScreenStub
      screenId="custom-lessons"
      phase={7}
      description="Create, edit and practice your own lesson modules, with JSON import/export for backup and sharing."
    />
  );
}
