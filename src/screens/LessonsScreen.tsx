import { ScreenStub } from "../components/ScreenStub";

/**
 * Stub — Lessons (curriculum browser: 250+ modules across 7 levels with
 * search, status filters and unlock states).
 * Design: `screens/all_lessons_typekernel/` — implemented in Phase 4.
 */
export default function LessonsScreen() {
  return (
    <ScreenStub
      screenId="lessons"
      phase={4}
      description="Curriculum browser: all lesson modules across levels with search, status filters and unlock states."
    />
  );
}
